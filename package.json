{
  "name": "jsonpath-router",
  "version": "0.1.0",
  "description": "An ExpressJS-inspired API for routing based on JSONPath expressions instead of URIs.",
  "main": "lib/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets es2015 --out-dir lib --ignore compiled --source-maps"
  },
  "keywords": [
    "json",
    "router",
    "routing",
    "jsonpath"
  ],
  "author": "Jiayu Yi <yi-jiayu@users.noreply.github.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2"
  },
  "dependencies": {
    "jsonpath": "^0.2.7"
  }
}
